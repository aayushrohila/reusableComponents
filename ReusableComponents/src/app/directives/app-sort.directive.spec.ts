import { AppSortDirective } from './app-sort.directive';

describe('AppSortDirective', () => {
  it('should create an instance', () => {
    const directive = new AppSortDirective();
    expect(directive).toBeTruthy();
  });
});
